trigger:
  branches:
    include:
      - master

pool: 
  vmImage: 'ubuntu-latest'

steps:
- task: Maven@3
  inputs:
    mavenPomFile: 'api/pom.xml'
    jdkVersionOption: '11‘
    goals: 'package'
